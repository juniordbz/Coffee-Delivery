import styled from 'styled-components'

export const CheckoutContainer = styled.form`
  display: flex;
  flex-wrap: wrap;
  height: auto;
  width: 100%;
  gap: 2rem;
  padding-bottom: 5rem;

  > div {
    display: flex;
    flex-wrap: wrap;
    flex-direction: column;
    gap: 1rem;
  }
`
